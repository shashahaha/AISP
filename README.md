
  # AISP

  This is a code bundle for AISP. The original project is available at https://www.figma.com/design/vjK4GoWyikC6tmlxQJSSdv/AISP.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  